name: Python CI

on:
  workflow_call:
    inputs:
      python-version:
        description: 'Python version to use.'
        required: false
        type: string
        default: '3.13'
      submodules:
        description: 'Enable git submodules checkout.'
        required: false
        type: boolean
        default: false
      lint-command:
        description: 'Command used to lint the project.'
        required: false
        type: string
        default: 'ruff check .'
      test-command:
        description: 'Command used to run tests.'
        required: false
        type: string
        default: 'pytest --verbose'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: ${{ inputs.submodules && 'recursive' || '' }}

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: ${{ inputs.python-version }}

      - name: Install dependencies
        run: |
          set -euo pipefail
          if [ -f pyproject.toml ]; then
            uv sync --all-extras
            echo "$PWD/.venv/bin" >> "$GITHUB_PATH"
          elif [ -f requirements.txt ]; then
            uv pip install --system -r requirements.txt ruff pytest
          else
            echo "::error::Neither pyproject.toml nor requirements.txt found."
            exit 1
          fi

      - name: Lint
        run: ${{ inputs.lint-command }}

      - name: Test
        run: ${{ inputs.test-command }}
