name: Self / Dependabot

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  merge:
    permissions:
      contents: write
      pull-requests: write
    uses: $/.github/workflows/ops-dependabot.yml
