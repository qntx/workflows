name: Self / Stale

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    permissions:
      issues: write
      pull-requests: write
    uses: $/.github/workflows/ops-stale.yml
