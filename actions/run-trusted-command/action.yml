name: run-trusted-command
description: Run a caller-supplied command via env, not template expansion in run.
inputs:
  command:
    description: 'Shell command to run. Passed through env, so it must come from a trusted source.'
    required: true
  working-directory:
    description: 'Directory to run the command in.'
    required: false
    default: '.'
runs:
  using: composite
  steps:
    - shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        CMD: ${{ inputs.command }}
      run: |
        if [ -z "${CMD}" ]; then
          echo '::error::run-trusted-command: command is empty'
          exit 1
        fi
        bash -c "$CMD" # zizmor: ignore[template-injection]
