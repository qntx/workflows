# Caller owns on:. Replace <sha> with a 40-character commit of qntx/workflows.
name: Stale

on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: qntx/workflows/.github/workflows/ops-stale.yml@<sha>
